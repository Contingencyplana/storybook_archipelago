def apply_camouflage(memory: dict) -> str:
	"""
	Minimal camouflage layer for Whispering Grove.
	Returns a human-readable string (no markers), strings only.
	"""
	return "A soft fog settles; the grove grows quiet."
