def describe_scene(memory: dict) -> str:
    # Must include "grove" somewhere in lowercased output per contract (allowing channel/pool too in tests)
    return "Where the channel kinks, a shoal bends light like a listening grove."
