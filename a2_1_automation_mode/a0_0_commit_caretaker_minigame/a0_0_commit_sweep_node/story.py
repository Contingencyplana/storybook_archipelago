def describe_scene(memory: dict) -> str:
    # Must include the word "grove" and no [PORTAL: tags
    return (
        "In the caretaker's grove, the tools rest on velvet.\n"
        "Left to plan, right to fit them."
    )
